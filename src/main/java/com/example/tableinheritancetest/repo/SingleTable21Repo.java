package com.example.tableinheritancetest.repo;

import com.example.tableinheritancetest.model.SingleTable21;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SingleTable21Repo extends JpaRepository<SingleTable21, Long> {
}